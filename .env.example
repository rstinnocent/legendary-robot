# Copy to .env (or just export the variable in your shell) and fill in your key.
# Groq's free tier needs no credit card: https://console.groq.com/keys
GROQ_API_KEY=your_key_here

# Only used if you switch the sidebar to the "Ollama (local)" backend instead.
# No key needed — just run `ollama serve` and `ollama pull llama3.2` first.
